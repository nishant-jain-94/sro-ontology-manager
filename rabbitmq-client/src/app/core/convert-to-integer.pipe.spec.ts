import { ConvertToIntegerPipe } from './convert-to-integer.pipe';

describe('ConvertToIntegerPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToIntegerPipe();
    expect(pipe).toBeTruthy();
  });
});
