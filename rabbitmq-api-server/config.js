var config = {};

config.rabbitMQconfig = {
    auth: {
        "auth": {
            "user": "guest",
            "pass": "guest"
        }
    },
    url: 'http://localhost:15672/api/'
};

module.exports = config;